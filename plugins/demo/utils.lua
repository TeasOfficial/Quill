-- ============================================================
-- demo/utils.lua - 公共工具函数
-- 被 init.lua 通过 include("utils") 加载
-- ============================================================

local M = {}  -- 模块表，存放要导出的函数

-- 返回格式化时间字符串
function M.now()
    return os.date("%Y-%m-%d %H:%M:%S")
end

return M  -- 返回模块表，调用方用 utils.xxx() 访问
